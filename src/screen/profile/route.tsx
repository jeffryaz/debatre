import Profile from './profile';

const RoutesProfile = [
    {
        path: 'Profile', name: 'profile', component: Profile, options: { headerShown: false, animation: 'slide_from_right', orientation: 'portrait' }
    },
];

export default RoutesProfile;
