import Search from './search';

const RoutesSearch = [
    {
        path: 'Search', name: 'search', component: Search, options: { headerShown: false, animation: 'slide_from_right', orientation: 'portrait' }
    },
];

export default RoutesSearch;
